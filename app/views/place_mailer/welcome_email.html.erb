<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
  </head>
  <body>
    <table class="table">
      <tr>
        <td>Custom rating</td>
        <td>Comment count</td>
        <td>Rate from map</td>
        <td>Name</td>
        <td>Address</td>
      </tr>
    <% @places_json.each do |place| %>
      <tr>
        <td><%= eval(sprintf("%8.2f",place['rating'])) %></td>
        <td><%= place['comment_count'] %></td>
        <td><%= place['rate'] %></td>
        <td><%= place['name'] %></td>
        <td><%= place['address'] %></td>
      </tr>
    <% end %>
  </body>
</html>
